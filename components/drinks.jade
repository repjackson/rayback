template(name='drink_categories')
    .ui.padded.stackable.grid.header-wrapper
        //- .ui.hidden.divider
        //- .row
        //-     .four.wide.column
        //-         h1.ui.massive.inline.header 
        //-             +i name='cocktail'
        //-             |drinks
        //-         .ui.icon.large.button.new_drink
        //-             i.green.plus.icon
        //-         +cloud filter='drink'
        //-     .twelve.wide.column
        //-         .ui.centered.three.cards
        //-             each drink_items
        //-                 +drink_card_template
        .ui.padded.stackable.grid
            each drink_categories
                .four.wide.column
                    .ui.segment
                        .ui.header
                            +i name=icon
                            | #{title}
                        i.thumbs.up.icon
                        |#{upvoters.length}
                        div
                        i.thumbs.down.icon
                        |#{downvoters.length}
                        
                        
template(name='drink_category')
    with current_doc
        .ui.padded.stackable.grid.header-wrapper
            .row
                .twelve.wide.column
                    h1.ui.header
                        +i name=icon
                        |#{title}
            .row
                .four.wide.column
                    if can_edit
                        a.ui.icon.button(href="/drink/#{_id}/edit" title='edit')
                            +i name='settings'
                    +array_view key='tags' icon='tags' direct=true
                    +array_view key='location_tags' label='location tags' icon='marker' direct=true
                    +array_view key='date_tags' label='date tags' icon='drinks' direct=true
                    div
                    +voting
                    +comments
                .twelve.wide.column
                    +youtube_view key='youtube_id' label='youtube id' direct=true
                    +html_view key='description' direct=true


                        
template(name='drink_card_template')
    .ui.card
        .image.drink_card.pointer
            img.ui.image(src="{{c.url image_id width=400 height=400 crop='fill'}}")
        .content
            .header.pointer.drink_card #{title}
        .content
            |location #{location}
            each tags
                .ui.basic.label #{this}
        .content
            div
            |#{orders_today} orders today
            div
            |#{orders_total} orders total
        .content
            .ui.fluid.green.button.order_now
                |order now        
                
template(name='drink_page')
    with current_doc
        .ui.padded.stackable.grid.header-wrapper
            .row
                .six.wide.column
                    h1.ui.header #{title}
                    +image_view key='image_id' label='image' direct=true
                    if can_edit
                        a.ui.icon.button(href="/drink/#{_id}/edit" title='edit')
                            +i name='settings'
                    +array_view key='tags' icon='tags' direct=true
                    +array_view key='location_tags' label='location tags' icon='marker' direct=true
                    +array_view key='date_tags' label='date tags' icon='drinks' direct=true
                    div
                    +comments
                .ten.wide.column
                    +youtube_view key='youtube_id' label='youtube id' direct=true
                    +html_view key='description' direct=true
                    +beer_info
                    .ui.segment
                        .ui.header 
                            +i name='clock'
                            |recent orders
                    .ui.segment
                        .ui.header 
                            +i name='user-shield'
                            |staff actions
                        +boolean_edit key='tapped' label='tapped' direct=true
                        +boolean_edit key='new' label='new' direct=true
                    .ui.segment
                        .ui.header 
                            +i name='ball-point-pen'
                            |reviews
                        .ui.button add review
                    .ui.segment
                        +voting_full

template(name='drink_edit')
    with current_doc
        .ui.padded.stackable.grid.header-wrapper
            .row
                .six.wide.column
                    .ui.inline.header #{title}
                    a.ui.large.basic.icon.button(href="/drink/#{_id}/view")
                        i.big.green.checkmark.icon
                    .ui.basic.icon.button.delete_drink_item
                        i.red.remove.icon
                    +text_edit key='title' icon='header' direct=true
                    +array_edit key='tags' icon='tags' direct=true
                    +array_edit key='location_tags' label='location tags' icon='marker' direct=true
                .ten.wide.column
                    +youtube_edit key='youtube_id' label='youtube id' direct=true
                    +html_edit key='description' direct=true



template(name='beer_info')
    .ui.segment
        .ui.header 
            +i name='info'
            |info
        .ui.list
            .item abv #{abv}
            .item glass price $#{glass_price}
            .item flight price $#{flight_price}
            .item ibu
        with type_doc
            .ui.header #{title}
        with company_doc
            .ui.header #{title}
